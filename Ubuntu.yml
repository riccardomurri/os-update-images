---

- include: '{{ host.ansible_os_family }}.yml'


- name: install packages (Ubuntu)
  apt:
    name: '{{ item }}'
    state: latest
  with_items:
    - python
    - python2.7
    - language-pack-en
    - language-pack-de
    - language-pack-fr
    - language-pack-it
